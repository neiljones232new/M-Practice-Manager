<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Statutory Accounts - {{company.name}}</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

        :root {
            --brand-primary: #6D28D9;
            --brand-muted: #EDE9FE;
            --text-primary: #0F172A;
            --text-secondary: #475569;
            --border-subtle: rgba(2,6,23,0.12);
        }

        @page {
            size: A4;
            margin: 10mm;
        }

        body {
            font-family: 'Poppins', Arial, sans-serif;
            font-size: 10.5pt;
            line-height: 1.4;
            margin: 0;
            padding: 32mm 14mm 25mm;
            color: var(--text-primary);
            background: #fff;
            -webkit-print-color-adjust: exact;
            color-adjust: exact;
        }

        p {
            margin: 6px 0;
        }

        ul {
            margin: 6px 0 6px 18px;
            padding: 0;
        }

        li {
            margin: 2px 0;
        }

        /* Cover */
        .cover {
            text-align: center;
            margin-top: 60mm;
        }

        .cover h1 {
            color: var(--brand-primary);
            font-size: 22pt;
            margin-bottom: 12px;
        }

        .cover .meta {
            font-size: 11pt;
            color: var(--text-secondary);
        }

        /* Page heading (static, non-floating) */
        .page-header {
            margin-top: 10mm;
            margin-bottom: 14px;
            padding-bottom: 6px;
            border-bottom: 2px solid var(--brand-primary);
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
        }

        .page-header .company {
            font-weight: 600;
            color: var(--brand-primary);
        }

        .page-header .period {
            font-size: 9.5pt;
            color: var(--text-secondary);
        }

        .section {
            margin: 12px 0;
            page-break-inside: avoid;
        }

        .section.page-break {
            page-break-before: always;
        }

        .section-title {
            font-size: 12pt;
            font-weight: 600;
            color: var(--brand-primary);
            margin: 12px 0 8px;
        }

        .financial-table {
            width: 100%;
            border-collapse: collapse;
            margin: 12px 0 20px;
        }

        /* Notes tables tighter + aligned like TaxCalc */
        .notes .financial-table th,
        .notes .financial-table td {
            padding: 5px 8px;
        }

        .notes .financial-table th:first-child,
        .notes .financial-table td:first-child {
            width: 60%;
        }

        .notes .financial-table th.amount,
        .notes .financial-table td.amount {
            width: 20%;
            text-align: right;
        }

        .financial-table th,
        .financial-table td {
            padding: 6px 8px;
            border-bottom: 1px solid var(--border-subtle);
        }

        .financial-table th {
            text-transform: uppercase;
            font-size: 9pt;
            color: var(--text-secondary);
            font-weight: 600;
            text-align: left;
        }

        .amount {
            text-align: right;
        }

        .subtotal-row {
            background: var(--brand-muted);
            font-weight: 600;
        }

        .total-row {
            border-top: 1.5px solid var(--brand-primary);
            font-weight: 700;
        }

        .indent {
            padding-left: 18px;
        }

        .notes h4 {
            color: var(--brand-primary);
            margin-top: 16px;
        }

        .signature-block {
            margin-top: 6px;
            break-inside: avoid;
            page-break-inside: avoid;
        }

        .signature-block p {
            margin: 4px 0;
        }

        .signature-line {
            border-bottom: 1.5px solid var(--text-primary);
            width: 200px;
            height: 12px;
        }

        @media print {
            body { padding: 0; }
            .page-break { page-break-before: always; }
            thead { display: table-header-group; }
            tfoot { display: table-footer-group; }
            tr { break-inside: avoid; page-break-inside: avoid; }
        }

        @media screen {
            body { background: #f6f5ff; padding: 40px; }
        }
    </style>
</head>
<body>

    <!-- Cover -->
    <div class="cover">
        <h1>{{company.name}}</h1>
        {{#if company.companyNumber}}
        <div class="meta">Company Registration Number: {{company.companyNumber}}</div>
        {{/if}}
        <div class="meta">Statutory Accounts for the {{#if period.isFirstYear}}period{{else}}year{{/if}} ended {{formatDate period.endDate}}</div>
    </div>

    <!-- Company Information -->
    <div class="section page-break">
        <div class="page-header">
            <div class="company">{{company.name}}</div>
            <div class="period">{{#if period.isFirstYear}}Period ended{{else}}Year ended{{/if}} {{formatDate period.endDate}}</div>
        </div>
        <div class="section-title">Company Information</div>
        <table class="financial-table">
            <tr>
                <th>Company name</th>
                <td>{{company.name}}</td>
            </tr>
            <tr>
                <th>Company registration number</th>
                <td>{{company.companyNumber}}</td>
            </tr>
            <tr>
                <th>Registered office</th>
                <td>
                    {{company.registeredOffice.line1}}{{#if company.registeredOffice.line2}}<br>{{company.registeredOffice.line2}}{{/if}}<br>
                    {{company.registeredOffice.town}}{{#if company.registeredOffice.county}}, {{company.registeredOffice.county}}{{/if}}<br>
                    {{company.registeredOffice.postcode}}{{#if company.registeredOffice.country}}<br>{{company.registeredOffice.country}}{{/if}}
                </td>
            </tr>
            <tr>
                <th>Directors</th>
                <td>
                    {{#each company.directors}}
                    {{this.name}}{{#unless @last}}<br>{{/unless}}
                    {{/each}}
                </td>
            </tr>
            <tr>
                <th>Accounting period</th>
                <td>{{formatDate period.startDate}} to {{formatDate period.endDate}}</td>
            </tr>
        </table>
    </div>

    <!-- Directors' Report -->
    {{#if frameworkDisclosures.includeDirectorsReport}}
    <div class="section page-break">
        <div class="page-header">
            <div class="company">{{company.name}}</div>
            <div class="period">{{#if period.isFirstYear}}Period ended{{else}}Year ended{{/if}} {{formatDate period.endDate}}</div>
        </div>
        <div class="section-title">Directors' Report</div>
        <p><strong>{{company.name}}</strong><br>Company No. {{company.companyNumber}}</p>
        <p>The directors present their report together with the financial statements for the {{#if period.isFirstYear}}period{{else}}year{{/if}} ended {{formatDate period.endDate}}.</p>
        <p><strong>Directors</strong><br>The directors who held office during the {{#if period.isFirstYear}}period{{else}}year{{/if}} were as follows:</p>
        <ul>
            {{#each company.directors}}
            <li>{{this.name}}</li>
            {{/each}}
        </ul>
        <p><strong>Statement of Directors' Responsibilities</strong></p>
        <p>The directors are responsible for preparing the Directors' Report and the financial statements in accordance with applicable law and regulations.</p>
        <p>Company law requires the directors to prepare financial statements for each financial year. Under that law the directors have elected to prepare the financial statements in accordance with United Kingdom Generally Accepted Accounting Practice (United Kingdom Accounting Standards and applicable law).</p>
        <p>Under company law the directors must not approve the financial statements unless they are satisfied that they give a true and fair view of the state of affairs of the company and of the profit or loss of the company for that period.</p>
        <p>In preparing the financial statements the directors are required to:</p>
        <ul>
            <li>Select suitable accounting policies and then apply them consistently;</li>
            <li>Make judgements and accounting estimates that are reasonable and prudent;</li>
            <li>Prepare the financial statements on the going concern basis unless it is inappropriate to presume that the company will continue in business.</li>
        </ul>
        <p>The directors are responsible for keeping adequate accounting records that are sufficient to show and explain the company's transactions and disclose with reasonable accuracy at any time the financial position of the company and enable them to ensure that the financial statements comply with the Companies Act 2006.</p>
        <p>They are also responsible for safeguarding the assets of the company and hence for taking reasonable steps for the prevention and detection of fraud and other irregularities.</p>
        <p>The directors are responsible for the maintenance and integrity of the corporate and financial information included on the company's website. Legislation in the United Kingdom governing the preparation and dissemination of financial statements may differ from legislation in other jurisdictions.</p>
        <p><strong>Small Company Rules</strong><br>This report has been prepared in accordance with the special provisions relating to companies subject to the small companies' regime within Part 15 of the Companies Act 2006.</p>
        <div class="signature-block">
        <p>On behalf of the board</p>
        {{#if directorsApproval.approved}}
        <p><strong>{{directorsApproval.directorName}}</strong><br>Director<br>{{formatDate directorsApproval.approvalDate}}</p>
        {{else}}
        <p><em>Awaiting directors' approval</em></p>
        {{/if}}
        </div>
    </div>
    {{/if}}

    <!-- Accountants' Report -->
    {{#if frameworkDisclosures.includeAccountantsReport}}
    <div class="section page-break">
        <div class="page-header">
            <div class="company">{{company.name}}</div>
            <div class="period">{{#if period.isFirstYear}}Period ended{{else}}Year ended{{/if}} {{formatDate period.endDate}}</div>
        </div>
        <div class="section-title">Accountants' Report</div>
        <p>In accordance with the engagement letter, and in order to assist the directors to fulfil their duties under the Companies Act 2006, we have compiled the financial statements of the company from the accounting records and information and explanations provided to us.</p>
        <p>This report is made to the directors in accordance with the terms of our engagement. Our work has been undertaken to prepare for approval by the directors the financial statements that we have been engaged to compile, to report to the directors that we have done so, and to state those matters that we have agreed to state to them in this report and for no other purpose.</p>
        <p>To the fullest extent permitted by law, we do not accept or assume responsibility to anyone other than the company and the company's directors for our work or for this report.</p>
        <p>You have acknowledged on the balance sheet your duty to ensure that the company has kept proper accounting records and to prepare financial statements that give a true and fair view under the Companies Act 2006. You consider that the company is exempt from the statutory requirement for an audit for the {{#if period.isFirstYear}}period{{else}}year{{/if}}.</p>
        <p>We have not been instructed to carry out an audit of the financial statements. For this reason, we have not verified the accuracy or completeness of the accounting records or information and explanations provided to us and do not, therefore, express any opinion on the financial statements.</p>
        <p><strong>Signed</strong></p>
        {{#if practice.name}}
        <p><strong>{{practice.name}}</strong><br>
            {{#each practice.addressLines}}{{this}}<br>{{/each}}
            {{#if directorsApproval.approved}}{{formatDate directorsApproval.approvalDate}}{{else}}{{formatDate period.endDate}}{{/if}}
        </p>
        {{/if}}
    </div>
    {{/if}}

    <!-- Profit and Loss -->
    {{#if frameworkDisclosures.includePLInClientPack}}
    <div class="section page-break">
        <div class="page-header">
            <div class="company">{{company.name}}</div>
            <div class="period">{{#if period.isFirstYear}}Period ended{{else}}Year ended{{/if}} {{formatDate period.endDate}}</div>
        </div>
        <div class="section-title">Profit and Loss Account</div>
        <table class="financial-table">
            <thead>
                <tr>
                    <td></td>
                    <td class="amount">{{formatDate period.endDate}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{formatDate comparatives.priorYear.endDate}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td></td>
                    <td class="amount">£</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">£</td>
                    {{/unless}}
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Turnover</td>
                    <td class="amount">{{formatCurrency profitAndLoss.lines.turnover}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if profitAndLoss.comparatives.priorYearLines}}{{formatCurrency profitAndLoss.comparatives.priorYearLines.turnover}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td>Cost of sales</td>
                    <td class="amount">({{formatCurrency profitAndLoss.lines.costOfSales}})</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if profitAndLoss.comparatives.priorYearLines}}({{formatCurrency profitAndLoss.comparatives.priorYearLines.costOfSales}}){{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="subtotal-row">
                    <td>Gross profit</td>
                    <td class="amount">{{formatCurrency calculations.profitAndLoss.grossProfit}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency comparatives.calculations.profitAndLoss.grossProfit}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td>Administrative expenses</td>
                    <td class="amount">({{formatCurrency calculations.profitAndLoss.totalExpenses}})</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}({{formatCurrency comparatives.calculations.profitAndLoss.totalExpenses}}){{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="subtotal-row">
                    <td>Operating profit</td>
                    <td class="amount">{{formatCurrency calculations.profitAndLoss.operatingProfit}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency comparatives.calculations.profitAndLoss.operatingProfit}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td>Interest payable</td>
                    <td class="amount">({{formatCurrency profitAndLoss.lines.interestPayable}})</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if profitAndLoss.comparatives.priorYearLines}}({{formatCurrency profitAndLoss.comparatives.priorYearLines.interestPayable}}){{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="subtotal-row">
                    <td>Profit before taxation</td>
                    <td class="amount">{{formatCurrency calculations.profitAndLoss.profitBeforeTax}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency comparatives.calculations.profitAndLoss.profitBeforeTax}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td>Tax on profit</td>
                    <td class="amount">({{formatCurrency profitAndLoss.lines.taxCharge}})</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if profitAndLoss.comparatives.priorYearLines}}({{formatCurrency profitAndLoss.comparatives.priorYearLines.taxCharge}}){{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="total-row">
                    <td>Profit for the financial {{#if period.isFirstYear}}period{{else}}year{{/if}}</td>
                    <td class="amount">{{formatCurrency calculations.profitAndLoss.profitAfterTax}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency comparatives.calculations.profitAndLoss.profitAfterTax}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td>Dividends declared</td>
                    <td class="amount">({{formatCurrency profitAndLoss.lines.dividendsDeclared}})</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if profitAndLoss.comparatives.priorYearLines}}({{formatCurrency profitAndLoss.comparatives.priorYearLines.dividendsDeclared}}){{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="subtotal-row">
                    <td>Retained profit for the financial {{#if period.isFirstYear}}period{{else}}year{{/if}}</td>
                    <td class="amount">{{formatCurrency (subtract calculations.profitAndLoss.profitAfterTax profitAndLoss.lines.dividendsDeclared)}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if profitAndLoss.comparatives.priorYearLines}}{{formatCurrency (subtract comparatives.calculations.profitAndLoss.profitAfterTax profitAndLoss.comparatives.priorYearLines.dividendsDeclared)}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
            </tbody>
        </table>

    </div>
    {{/if}}

    <!-- Balance Sheet -->
    <div class="section page-break">
        <div class="page-header">
            <div class="company">{{company.name}}</div>
            <div class="period">{{#if period.isFirstYear}}Period ended{{else}}Year ended{{/if}} {{formatDate period.endDate}}</div>
        </div>
        <div class="section-title">Balance Sheet</div>
        <table class="financial-table">
            <thead>
                <tr>
                    <td></td>
                    <td class="amount">{{formatDate period.endDate}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{formatDate comparatives.priorYear.endDate}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td></td>
                    <td class="amount">£</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">£</td>
                    {{/unless}}
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><strong>Fixed assets</strong></td>
                    <td></td>
                    {{#unless period.isFirstYear}}
                    <td></td>
                    {{/unless}}
                </tr>
                <tr>
                    <td class="indent">Tangible assets</td>
                    <td class="amount">{{formatCurrency balanceSheet.assets.fixedAssets.tangibleFixedAssets}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if balanceSheet.comparatives.prior}}{{formatCurrency balanceSheet.comparatives.prior.assets.fixedAssets.tangibleFixedAssets}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="subtotal-row">
                    <td>Total fixed assets</td>
                    <td class="amount">{{formatCurrency calculations.balanceSheet.totalFixedAssets}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency comparatives.calculations.balanceSheet.totalFixedAssets}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td><strong>Current assets</strong></td>
                    <td></td>
                    {{#unless period.isFirstYear}}
                    <td></td>
                    {{/unless}}
                </tr>
                <tr>
                    <td class="indent">Stocks</td>
                    <td class="amount">{{formatCurrency balanceSheet.assets.currentAssets.stock}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if balanceSheet.comparatives.prior}}{{formatCurrency balanceSheet.comparatives.prior.assets.currentAssets.stock}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td class="indent">Debtors</td>
                    <td class="amount">{{formatCurrency balanceSheet.assets.currentAssets.debtors}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if balanceSheet.comparatives.prior}}{{formatCurrency balanceSheet.comparatives.prior.assets.currentAssets.debtors}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td class="indent">Cash at bank and in hand</td>
                    <td class="amount">{{formatCurrency balanceSheet.assets.currentAssets.cash}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if balanceSheet.comparatives.prior}}{{formatCurrency balanceSheet.comparatives.prior.assets.currentAssets.cash}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="subtotal-row">
                    <td>Total current assets</td>
                    <td class="amount">{{formatCurrency calculations.balanceSheet.totalCurrentAssets}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency comparatives.calculations.balanceSheet.totalCurrentAssets}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td><strong>Creditors: amounts falling due within one year</strong></td>
                    <td class="amount">({{formatCurrency calculations.balanceSheet.totalCurrentLiabilities}})</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}({{formatCurrency comparatives.calculations.balanceSheet.totalCurrentLiabilities}}){{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="subtotal-row">
                    <td><strong>Net current assets / (liabilities)</strong></td>
                    <td class="amount">{{formatCurrency (subtract calculations.balanceSheet.totalCurrentAssets calculations.balanceSheet.totalCurrentLiabilities)}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency (subtract comparatives.calculations.balanceSheet.totalCurrentAssets comparatives.calculations.balanceSheet.totalCurrentLiabilities)}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="subtotal-row">
                    <td><strong>Total assets less current liabilities</strong></td>
                    <td class="amount">{{formatCurrency (subtract (add calculations.balanceSheet.totalFixedAssets calculations.balanceSheet.totalCurrentAssets) calculations.balanceSheet.totalCurrentLiabilities)}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency (subtract (add comparatives.calculations.balanceSheet.totalFixedAssets comparatives.calculations.balanceSheet.totalCurrentAssets) comparatives.calculations.balanceSheet.totalCurrentLiabilities)}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                {{#if (gt calculations.balanceSheet.totalLongTermLiabilities 0)}}
                <tr>
                    <td><strong>Creditors: amounts falling due after more than one year</strong></td>
                    <td class="amount">({{formatCurrency calculations.balanceSheet.totalLongTermLiabilities}})</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}({{formatCurrency comparatives.calculations.balanceSheet.totalLongTermLiabilities}}){{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                {{/if}}
                <tr class="total-row">
                    <td>Net assets</td>
                    <td class="amount">{{formatCurrency calculations.balanceSheet.netAssets}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency comparatives.calculations.balanceSheet.netAssets}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td><strong>Capital and reserves</strong></td>
                    <td></td>
                    {{#unless period.isFirstYear}}
                    <td></td>
                    {{/unless}}
                </tr>
                <tr>
                    <td class="indent">Called up share capital</td>
                    <td class="amount">{{formatCurrency balanceSheet.equity.shareCapital}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if balanceSheet.comparatives.prior}}{{formatCurrency balanceSheet.comparatives.prior.equity.shareCapital}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr>
                    <td class="indent">Profit and loss account</td>
                    <td class="amount">{{formatCurrency balanceSheet.equity.retainedEarnings}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if balanceSheet.comparatives.prior}}{{formatCurrency balanceSheet.comparatives.prior.equity.retainedEarnings}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
                <tr class="total-row">
                    <td>Shareholders' funds</td>
                    <td class="amount">{{formatCurrency calculations.balanceSheet.totalEquity}}</td>
                    {{#unless period.isFirstYear}}
                    <td class="amount">{{#if comparatives.calculations}}{{formatCurrency comparatives.calculations.balanceSheet.totalEquity}}{{else}}-{{/if}}</td>
                    {{/unless}}
                </tr>
            </tbody>
        </table>

        {{#if frameworkDisclosures.auditExemption.isAuditExempt}}
        <p class="notes">For the {{#if period.isFirstYear}}period{{else}}year{{/if}} ended {{formatDate period.endDate}} the company was entitled to exemption from audit under section 477 of the Companies Act 2006 relating to small companies.</p>
        <p class="notes">The members have not required the company to obtain an audit in accordance with section 476 of the Companies Act 2006.</p>
        <p class="notes">The directors acknowledge their responsibilities for complying with the requirements of the Companies Act 2006 with respect to accounting records and the preparation of accounts.</p>
        <p class="notes">These accounts have been prepared in accordance with the provisions applicable to companies subject to the small companies' regime.</p>
        {{/if}}

        <div class="signature-block">
        <p class="notes">On behalf of the board</p>
        {{#if directorsApproval.approved}}
        <div style="margin-top: 8px;">
            <div class="signature-line"></div>
            <p><strong>{{directorsApproval.directorName}}</strong></p>
            <p>Director</p>
            <p>{{formatDate directorsApproval.approvalDate}}</p>
        </div>
        {{else}}
        <p class="notes"><em>Awaiting directors' approval</em></p>
        {{/if}}
        </div>

        <p class="notes" style="margin-top: 10px;">The notes on the following pages form part of these financial statements.</p>
    </div>

    <!-- Notes -->
    <div class="section page-break notes">
        <div class="page-header">
            <div class="company">{{company.name}}</div>
            <div class="period">{{#if period.isFirstYear}}Period ended{{else}}Year ended{{/if}} {{formatDate period.endDate}}</div>
        </div>
        <div class="section-title">Notes to the Accounts</div>
        <h4>1. General information</h4>
        <p>{{company.name}} is a private company limited by shares, incorporated in {{#if notes.countryOfIncorporation}}{{notes.countryOfIncorporation}}{{else}}England and Wales{{/if}}. The registered office is {{company.registeredOffice.line1}}{{#if company.registeredOffice.line2}}, {{company.registeredOffice.line2}}{{/if}}, {{company.registeredOffice.town}}{{#if company.registeredOffice.county}}, {{company.registeredOffice.county}}{{/if}}, {{company.registeredOffice.postcode}}.</p>

        <h4>2. Accounting policies</h4>
        <p><strong>Basis of preparation</strong><br>{{#if accountingPolicies.basisOfPreparation}}{{accountingPolicies.basisOfPreparation}}{{else}}The financial statements have been prepared under the historical cost convention and in accordance with FRS 102 Section 1A Small Entities and the Companies Act 2006.{{/if}}</p>

        {{#if accountingPolicies.goingConcern.isGoingConcern}}
        <p><strong>Going concern:</strong> {{#if accountingPolicies.goingConcern.noteText}}{{accountingPolicies.goingConcern.noteText}}{{else}}The accounts have been prepared on a going concern basis.{{/if}}</p>
        {{/if}}

        {{#if accountingPolicies.turnoverPolicyText}}
        <p><strong>Turnover</strong><br>{{accountingPolicies.turnoverPolicyText}}</p>
        {{/if}}

        {{#if accountingPolicies.tangibleFixedAssets.hasAssets}}
        <p><strong>Tangible fixed assets and depreciation</strong><br>Depreciation is provided on a {{accountingPolicies.tangibleFixedAssets.depreciationMethod}} basis.</p>
        {{/if}}

        <h4>3. Share capital</h4>
        <p>{{notes.shareCapital.numberOfShares}} {{notes.shareCapital.shareClass}} shares of {{formatCurrency notes.shareCapital.nominalValue}} each.</p>

        {{#if notes.employees.include}}
        <h4>4. Employees</h4>
        <p>The average number of employees, including directors, during the {{#if period.isFirstYear}}period{{else}}year{{/if}} was {{notes.employees.averageEmployees}}.</p>
        {{/if}}

        {{#if notes.tangibleAssets}}
        <h4>5. Tangible assets</h4>
        <table class="financial-table">
            <thead>
                <tr>
                    <th></th>
                    {{#each notes.tangibleAssets.columns}}
                    <th class="amount">{{this}}</th>
                    {{/each}}
                </tr>
            </thead>
            <tbody>
                {{#each notes.tangibleAssets.rows}}
                <tr{{#if (or (eq this.label "Cost c/fwd") (eq this.label "Depreciation c/fwd") (eq this.label "Net book value (current)"))}} class="subtotal-row"{{/if}}>
                    <td>
                        {{#if (eq this.label "Net book value (current)")}}
                          Net book value ({{formatDate ../period.endDate 'YYYY'}})
                        {{else}}
                          {{#if (eq this.label "Net book value (prior)")}}
                            Net book value ({{#if ../comparatives.priorYear.endDate}}{{formatDate ../comparatives.priorYear.endDate 'YYYY'}}{{else}}{{formatDate ../period.endDate 'YYYY'}}{{/if}})
                          {{else}}
                            {{this.label}}
                          {{/if}}
                        {{/if}}
                    </td>
                    {{#each this.values}}
                    <td class="amount">{{formatCurrency this}}</td>
                    {{/each}}
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{/if}}

        {{#if notes.principalActivity}}
        <h4>6. Principal Activity</h4>
        <p>{{notes.principalActivity}}</p>
        {{/if}}

        {{#if frameworkDisclosures.auditExemption.isAuditExempt}}
        <h4>7. Audit Exemption</h4>
        <p>
            The company is entitled to exemption from audit under
            {{#if (eq frameworkDisclosures.auditExemption.exemptionStatementKey "MICRO_ENTITY")}}the micro-entity provisions{{/if}}
            {{#if (eq frameworkDisclosures.auditExemption.exemptionStatementKey "CA2006_S477_SMALL")}}section 477 of the Companies Act 2006 relating to small companies{{/if}}
            {{#if (eq frameworkDisclosures.auditExemption.exemptionStatementKey "DORMANT")}}the dormant company provisions{{/if}}.
        </p>
        {{/if}}

        {{#if notes.additionalNotes}}
        {{#each notes.additionalNotes}}
        <h4>Additional Note: {{this.title}}</h4>
        <p>{{this.text}}</p>
        {{/each}}
        {{/if}}
    </div>

    <!-- Directors' Approval -->
    <div class="section page-break">
        <div class="page-header">
            <div class="company">{{company.name}}</div>
            <div class="period">{{#if period.isFirstYear}}Period ended{{else}}Year ended{{/if}} {{formatDate period.endDate}}</div>
        </div>
        <div class="section-title">Directors' Approval</div>
        {{#if directorsApproval.approved}}
        <p>Approved and authorised for issue on {{formatDate directorsApproval.approvalDate}}.</p>
        <p><strong>{{directorsApproval.directorName}}</strong><br>Director</p>
        {{else}}
        <p><em>Awaiting directors' approval</em></p>
        {{/if}}
    </div>

</body>
</html>
